

LFS_MULTIPART_UPLOAD_COMMAND = ...
def install_lfs_in_userspace(): # -> None:
    """
    If in Linux, installs git-lfs in userspace
    (sometimes useful if you can't `sudo apt install` or equivalent).
    """
    ...

