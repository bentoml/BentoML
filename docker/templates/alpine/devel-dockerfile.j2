{{ header }}
{% set cached = ['arm32v5', 'arm32v6'] %}
ARG PYTHON_VERSION

FROM --platform=$BUILDPLATFORM {{ organization }}/{{ base_tag }} as base

SHELL ["/bin/bash", "-eo", "pipefail", "-c"]

ENV BENTOML_VERSION={{ envars['BENTOML_VERSION'] }}

RUN --mount=type=cache,target=/var/cache/apk \
    xx-apk --update add build-base git musl-dev linux-headers g++

RUN --mount=type=cache,mode=0777,target=/root/.cache/pip \
    pip install -U git+https://github.com/bentoml/bentoml.git@main
{% for key in architectures if key not in cached -%}
    {% if 'arm64' in key %}

FROM base as base-arm64
    {% elif 'arm' in key %}

FROM base as base-arm
    {% elif 'i386' in key %}

FROM base as base-386
    {% else %}

FROM base as base-{{ key }}
    {% endif %}
{% endfor %}

FROM base-${TARGETARCH}

ARG TARGETARCH

ARG TARGETPLATFORM

