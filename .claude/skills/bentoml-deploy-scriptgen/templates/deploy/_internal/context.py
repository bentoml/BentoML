"""RunContext: everything a target module needs for one deploy run.

Generated by the bentoml-deploy-scriptgen skill — do not edit by hand.
"""

# This bundle gets committed into arbitrary user repos; the directive below
# keeps the import layout stable under any repo's ruff/isort configuration.
# isort: skip_file
from __future__ import annotations

from dataclasses import dataclass

from .common import Summary
from .config import Config


@dataclass
class RunContext:
    cfg: Config
    target: str
    # None only while the default git-SHA tag could not be derived — the
    # preflight report then fails on common.git-repo before image is needed.
    image: str | None
    version: str | None
    check_only: bool
    skip_build: bool
    no_verify: bool
    summary: Summary
    local_only: bool = False
