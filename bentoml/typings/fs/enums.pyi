from enum import unique
from enum import IntEnum

@unique
class ResourceType(IntEnum):
    unknown = ...
    directory = ...
    file = ...
    character = ...
    block_special_file = ...
    fifo = ...
    socket = ...
    symlink = ...

@unique
class Seek(IntEnum):
    current = ...
    end = ...
    set = ...
