# syntax = docker/dockerfile:1.3-labs
#
# ===========================================
#
# THIS IS A GENERATED DOCKERFILE. DO NOT EDIT
#
# ===========================================

ARG PYTHON_VERSION

FROM --platform=$BUILDPLATFORM aarnphm/bento-server:base-python$PYTHON_VERSION-debian10-conda as base

ENV BENTOML_VERSION=1.0.0a6

ARG MAMBA_DOCKERFILE_ACTIVATE 1

RUN pip install bentoml==${BENTOML_VERSION} --no-cache-dir

RUN xx-apt-get purge --auto-remove -y gcc python3-dev \
    && xx-apt-get clean \
    && rm -rf /var/lib/{apt,dpkg,cache,log}

FROM base as base-amd64

FROM base as base-arm64

FROM base as base-ppc64le

FROM base-${TARGETARCH}

ARG TARGETARCH

ARG TARGETPLATFORM