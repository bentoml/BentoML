language: python

dist: trusty

cache: pip

python:
  - 2.7
  - 3.4
  - 3.6
  - 3.7

install:
  - "pip install --upgrade pip"
  - "pip install -e .[test]"
  - "pip install ."

script:
  - pytest --cov=bentoml

after_success:
  - codecov
