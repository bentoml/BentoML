{{- if .Values.usePostgres -}}
apiVersion: v1
kind: Service
metadata:
  name: postgres
  labels:
    app: postgres
spec:
  type: NodePort
  ports:
    - port: {{ .Values.postgres.port }}
      nodePort: 30007
  selector:
   app: postgres
{{ end }}