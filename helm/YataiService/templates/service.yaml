apiVersion: v1
kind: Service
metadata:
  name: {{ include "YataiService.fullname" . }}
  labels:
    {{- include "YataiService.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "YataiService.selectorLabels" . | nindent 4 }}
