import ctypes
import select

SYSTEM = ...
if SYSTEM == "Darwin" and ctypes is not None:
    libSystem = ...
    CoreServices = ...
    mach_absolute_time = ...
    absolute_to_nanoseconds = ...
    def monotonic(): ...

else:
    CLOCK_MONOTONIC = ...
    class timespec(ctypes.Structure):
        _fields_ = ...
    librt = ...
    clock_gettime = ...
    def monotonic(): ...

if hasattr(select, "poll"): ...
else: ...
