# Separate out the tests requirements to vendor the requirements
# so that external libraries can use internal py_test rules.
pytest-cov==4.0.0
pytest==7.2.0
pytest-xdist[psutil]==3.0.2
pytest-asyncio==0.20.2
coverage[toml]==6.5.0
yamllint==1.28.0
black==22.10.0
codecov==2.1.12
setuptools==65.5.1
isort==5.10.1
pylint==2.15.5
imageio==2.22.4
build[virtualenv]==0.9.0
tomli==2.0.1
setuptools-scm[toml]==7.0.5
